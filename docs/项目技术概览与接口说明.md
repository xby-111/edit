# 项目技术概览与接口说明

## 1. 项目整体概况

本项目是一个基于 FastAPI 的**协作编辑器后端系统**，支持多用户实时协作编辑文档。系统提供完整的用户认证、权限管理、文档版本控制等功能，并通过 WebSocket 实现实时协作编辑能力。项目面向需要多人协作编辑文档的场景，如团队文档协作、在线编辑器等应用场景。

**主要技术栈：**
- **后端框架**：FastAPI 0.121.2
- **编程语言**：Python 3.10+
- **数据库**：OpenGauss（兼容 PostgreSQL 协议）
- **ORM**：SQLAlchemy 2.0.44
- **认证方式**：JWT Token（python-jose）
- **密码加密**：bcrypt（passlib）
- **实时通信**：WebSocket（websockets 15.0.1）
- **Web服务器**：Uvicorn 0.38.0
- **数据验证**：Pydantic（pydantic-settings 2.0.3）
- **模板引擎**：Jinja2 3.1.4
- **CRDT算法**：已实现基础框架但尚未集成到协作编辑中

**核心目录结构：**
- `app/api/routers/` - API 路由层，定义所有 HTTP 接口和 WebSocket 端点
- `app/services/` - 业务逻辑层，封装核心业务处理逻辑
- `models/` - 数据模型层，定义 SQLAlchemy ORM 模型
- `schemas/` - 数据验证层，定义 Pydantic 数据模型和请求/响应结构
- `app/db/` - 数据库相关，包含连接配置、初始化脚本
- `app/core/` - 核心配置和安全模块，包含应用配置、JWT 认证、密码加密等
- `utils/` - 工具函数，包含异常定义、响应格式化等
- `static/` - 静态文件目录（JavaScript、CSS 等）
- `templates/` - HTML 模板文件（index.html、test_collab.html）
- `app/` - 应用主入口和 CRDT 相关代码

**项目架构特点：**
- 采用分层架构，清晰分离关注点
- Service 层不直接提交事务，由 Router 层统一控制
- WebSocket 实现实时协作编辑，支持光标位置同步
- 完整的权限控制系统（RBAC + ACL）
- 支持文档版本控制和操作日志记录

---

## 2. 数据库与数据模型

### 2.1 数据库连接配置

**数据库类型**：OpenGauss（使用 PostgreSQL 方言）

**连接配置位置**：`core/config.py`

**连接字符串格式**：
```
postgresql+psycopg2://用户名:密码@主机:端口/数据库名
```

**默认配置**（可通过环境变量 `DATABASE_URL` 覆盖）：
- 默认连接：`postgresql+psycopg2://appuser:***@localhost:5432/editdb`
- 连接池配置（`db/session.py`）：
  - `pool_size`: 10
  - `max_overflow`: 20
  - `pool_pre_ping`: True（连接前验证有效性）

### 2.2 数据库表结构

#### 2.2.1 用户表 (users)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY, AUTO_INCREMENT | 用户ID |
| username | String(50) | UNIQUE, INDEX, NOT NULL | 用户名 |
| email | String(100) | UNIQUE, INDEX | 邮箱地址 |
| phone | String(20) | UNIQUE, INDEX | 手机号 |
| hashed_password | String(255) | NOT NULL | 密码哈希值 |
| is_active | Boolean | DEFAULT=True | 账户是否激活 |
| role | String(20) | DEFAULT="viewer" | 用户角色：admin/editor/viewer |
| avatar_url | String(500) | | 头像URL |
| full_name | String(100) | | 全名 |
| bio | Text | | 个人简介 |
| address | String(500) | | 地址 |
| phone_secondary | String(20) | | 备用电话 |
| password_reset_token | String(255) | | 密码重置令牌 |
| password_reset_expires | DateTime | | 密码重置过期时间 |
| verification_code | String(10) | | 验证码 |
| verification_code_expires | DateTime | | 验证码过期时间 |
| created_at | DateTime | NOT NULL, DEFAULT=now() | 创建时间 |
| updated_at | DateTime | NOT NULL, DEFAULT=now(), ONUPDATE=now() | 更新时间 |

**关系**：
- `documents` - 一对多关系，一个用户可以有多个文档
- `operation_logs` - 一对多关系，一个用户可以有多个操作日志

**约束**：
- `username`、`email`、`phone` 分别有唯一约束和索引
- 邮箱和手机号至少提供一个（应用层验证）

#### 2.2.2 文档表 (documents)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY, AUTO_INCREMENT | 文档ID |
| owner_id | Integer | FOREIGN KEY(users.id), NOT NULL | 文档所有者ID |
| title | String(200) | INDEX, NOT NULL | 文档标题 |
| content | Text | NOT NULL, DEFAULT="" | 文档内容 |
| status | String(50) | DEFAULT="active" | 文档状态：active/archived |
| created_at | DateTime | NOT NULL, DEFAULT=now() | 创建时间 |
| updated_at | DateTime | NOT NULL, DEFAULT=now(), ONUPDATE=now() | 更新时间 |

**关系**：
- `owner` - 多对一关系，文档属于一个用户
- `versions` - 一对多关系，一个文档可以有多个版本

#### 2.2.3 文档版本表 (document_versions)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY, AUTO_INCREMENT | 版本ID |
| document_id | Integer | FOREIGN KEY(documents.id), NOT NULL | 文档ID |
| user_id | Integer | FOREIGN KEY(users.id), NOT NULL | 创建版本的用户ID |
| version_number | Integer | NOT NULL | 版本号 |
| content_snapshot | Text | NOT NULL | 内容快照 |
| summary | String(500) | | 变更摘要 |
| created_at | DateTime | NOT NULL, DEFAULT=now() | 创建时间 |

**关系**：
- `document` - 多对一关系，版本属于一个文档

#### 2.2.4 操作日志表 (operation_logs)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY, AUTO_INCREMENT | 日志ID |
| user_id | Integer | FOREIGN KEY(users.id), NOT NULL | 操作用户ID |
| action | String(100) | NOT NULL | 操作类型：login, logout, create_document, update_document等 |
| resource_type | String(50) | | 资源类型：document, user等 |
| resource_id | Integer | | 资源ID |
| description | Text | | 操作描述 |
| ip_address | String(50) | | IP地址 |
| user_agent | String(500) | | 用户代理 |
| created_at | DateTime | NOT NULL, DEFAULT=now() | 创建时间 |

**关系**：
- `user` - 多对一关系，日志属于一个用户

#### 2.2.5 权限表 (permissions)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY, AUTO_INCREMENT | 权限ID |
| name | String(100) | UNIQUE, NOT NULL | 权限名称：read_document, write_document等 |
| description | Text | | 权限描述 |
| resource_type | String(50) | | 资源类型：document, user, system |
| created_at | DateTime | NOT NULL, DEFAULT=now() | 创建时间 |

**约束**：
- `name` 有唯一约束

#### 2.2.6 角色权限关联表 (role_permissions)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY, AUTO_INCREMENT | 关联ID |
| role | String(20) | NOT NULL | 角色：admin, editor, viewer |
| permission_id | Integer | FOREIGN KEY(permissions.id), NOT NULL | 权限ID |
| created_at | DateTime | NOT NULL, DEFAULT=now() | 创建时间 |

**用途**：建立角色与权限的多对多关系

#### 2.2.7 访问控制列表 (acls)

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PRIMARY KEY, AUTO_INCREMENT | ACL ID |
| resource_type | String(50) | NOT NULL | 资源类型：document等 |
| resource_id | Integer | NOT NULL | 资源ID |
| user_id | Integer | FOREIGN KEY(users.id) | 用户ID（为空则对所有用户） |
| role | String(20) | | 角色（为空则对所有角色） |
| permission | String(100) | NOT NULL | 权限：read, write, delete等 |
| granted | Boolean | DEFAULT=True | 是否授予权限 |
| created_at | DateTime | NOT NULL, DEFAULT=now() | 创建时间 |
| updated_at | DateTime | NOT NULL, DEFAULT=now(), ONUPDATE=now() | 更新时间 |

**用途**：细粒度的资源级权限控制

### 2.3 外键关系

- documents.owner_id → users.id
- document_versions.document_id → documents.id
- document_versions.user_id → users.id
- operation_logs.user_id → users.id
- role_permissions.permission_id → permissions.id
- acls.user_id → users.id

### 2.4 核心数据模型

所有模型定义在 `models/__init__.py` 文件中，使用 SQLAlchemy ORM。

---

## 3. 对外 HTTP API / 接口说明

所有 API 接口前缀为 `/api/v1`，定义在 `app/main.py` 中注册路由。

### 3.1 认证相关接口

**路由文件**：`api/routers/auth.py`

#### POST `/api/v1/auth/register`

**功能**：用户注册

**请求体**（JSON）：
```json
{
  "username": "string",      // 必填，用户名
  "email": "string",          // 可选，邮箱格式
  "phone": "string",          // 可选，手机号格式
  "password": "string",       // 必填，密码
  "is_active": true,          // 可选，默认true
  "role": "viewer"            // 可选，默认viewer
}
```

**响应**（201 Created）：
```json
{
  "id": 1,
  "username": "string",
  "email": "string",
  "phone": "string",
  "is_active": true,
  "role": "viewer",
  "created_at": "2024-01-01T00:00:00"
}
```

**验证规则**：
- 邮箱或手机号至少提供一个（Pydantic 模型验证）
- 用户名、邮箱、手机号不能重复（数据库唯一约束）
- 密码长度至少6个字符（前端验证）

**认证要求**：无需认证

**实现位置**：`app/api/routers/auth.py:register()`

---

#### POST `/api/v1/auth/token`

**功能**：用户登录（OAuth2 兼容，表单格式）

**请求格式**：`application/x-www-form-urlencoded`

**请求参数**：
- `username`: 用户名（当前仅支持用户名，不支持邮箱或手机号）
- `password`: 密码

**响应**（200 OK）：
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

**认证要求**：无需认证

**实现位置**：`app/api/routers/auth.py:login()`

**说明**：当前实现仅支持通过用户名登录，不支持邮箱或手机号登录

---

#### GET `/api/v1/auth/me`

**功能**：获取当前登录用户信息

**响应**（200 OK）：
```json
{
  "id": 1,
  "username": "string",
  "email": "string",
  "is_active": true
}
```

**认证要求**：需要 Bearer Token（JWT）

**实现位置**：`app/api/routers/auth.py:read_users_me()`

**说明**：当前实现仅返回基本用户信息（id、username、email、is_active），不包含完整个人资料

---

#### POST `/api/v1/auth/password-reset/request`

**功能**：请求密码重置

**请求体**（JSON）：
```json
{
  "email": "user@example.com"
}
```

**响应**（200 OK）：
```json
{
  "message": "密码重置邮件已发送"
}
```

**认证要求**：无需认证

---

#### POST `/api/v1/auth/password-reset/verify`

**功能**：验证重置令牌并更新密码

**请求体**（JSON）：
```json
{
  "token": "reset_token_string",
  "new_password": "new_secure_password"
}
```

**响应**（200 OK）：
```json
{
  "message": "密码重置成功"
}
```

---

#### POST `/api/v1/auth/login/code/request`

**功能**：请求验证码登录

**请求体**（JSON）：
```json
{
  "email": "user@example.com"  // 或 "phone": "+86138xxxxxxxx"
}
```

**响应**（200 OK）：
```json
{
  "message": "验证码已发送"
}
```

---

#### POST `/api/v1/auth/login/code/verify`

**功能**：使用验证码登录

**请求体**（JSON）：
```json
{
  "email": "user@example.com",
  "code": "123456"
}
```

**响应**（200 OK）：
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

---

#### GET `/api/v1/auth/oauth/providers`

**功能**：获取支持的 OAuth2 提供商列表

**响应**（200 OK）：
```json
{
  "providers": ["github", "google", "wechat"]
}
```

---

#### GET `/api/v1/auth/oauth/{provider}/authorize`

**功能**：获取 OAuth2 授权 URL

**路径参数**：
- `provider` (string) - 提供商名称（github/google/wechat）

**响应**（200 OK）：
```json
{
  "authorization_url": "https://github.com/login/oauth/authorize?..."
}
```

---

#### POST `/api/v1/auth/oauth/{provider}/callback`

**功能**：OAuth2 回调处理

**请求体**（JSON）：
```json
{
  "code": "authorization_code"
}
```

**响应**（200 OK）：
```json
{
  "access_token": "jwt_token",
  "token_type": "bearer",
  "user": { ... }
}
```

---

#### GET `/api/v1/auth/oauth/accounts`

**功能**：获取已绑定的 OAuth 账户

**认证要求**：需要 Bearer Token

**响应**（200 OK）：
```json
{
  "accounts": [
    {"provider": "github", "provider_user_id": "12345", "bound_at": "2024-01-01T00:00:00"}
  ]
}
```

---

#### DELETE `/api/v1/auth/oauth/{provider}`

**功能**：解绑 OAuth 账户

**认证要求**：需要 Bearer Token

---

#### GET `/api/v1/auth/2fa/status`

**功能**：获取当前用户的 2FA 状态

**认证要求**：需要 Bearer Token

**响应**（200 OK）：
```json
{
  "enabled": false,
  "backup_codes_remaining": 0
}
```

---

#### POST `/api/v1/auth/2fa/setup`

**功能**：设置 2FA（获取密钥和二维码）

**认证要求**：需要 Bearer Token

**响应**（200 OK）：
```json
{
  "secret": "JBSWY3DPEHPK3PXP",
  "qr_code": "data:image/png;base64,..."
}
```

---

#### POST `/api/v1/auth/2fa/enable`

**功能**：验证并启用 2FA

**请求体**（JSON）：
```json
{
  "code": "123456"
}
```

**响应**（200 OK）：
```json
{
  "message": "2FA 已启用",
  "backup_codes": ["CODE1", "CODE2", ...]
}
```

---

#### POST `/api/v1/auth/2fa/disable`

**功能**：禁用 2FA

**请求体**（JSON）：
```json
{
  "code": "123456"
}
```

---

#### POST `/api/v1/auth/2fa/backup-codes/regenerate`

**功能**：重新生成备用码

**认证要求**：需要 Bearer Token + 2FA 验证

---

#### POST `/api/v1/auth/token/2fa`

**功能**：带 2FA 验证的登录

**请求格式**：`application/x-www-form-urlencoded`

**请求参数**：
- `username`: 用户名
- `password`: 密码
- `totp_code`: 2FA 验证码

---

### 3.2 用户管理接口

**路由文件**：`api/routers/users.py`

#### GET `/api/v1/users/{user_id}`

**功能**：获取指定用户信息

**路径参数**：
- `user_id` (int) - 用户ID

**响应**（200 OK）：
```json
{
  "id": 1,
  "username": "string",
  "email": "string",
  "phone": "string",
  "is_active": true,
  "role": "viewer",
  "full_name": "string",
  "bio": "string",
  "avatar_url": "string",
  "created_at": "2024-01-01T00:00:00",
  "updated_at": "2024-01-01T00:00:00"
}
```

**认证要求**：需要 Bearer Token

**权限要求**：无特殊权限要求（会记录操作日志）

**实现位置**：`app/api/routers/users.py:read_user()`

---

#### PUT `/api/v1/users/{user_id}`

**功能**：更新用户信息

**路径参数**：
- `user_id` (int) - 用户ID

**请求体**（JSON）：
```json
{
  "username": "string",        // 可选
  "email": "string",           // 可选
  "is_active": true,           // 可选
  "role": "string"            // 可选
}
```

**响应**（200 OK）：更新后的 User 对象

**认证要求**：需要 Bearer Token

**权限要求**：只能更新自己的信息，或需要 admin 角色

**实现位置**：`app/api/routers/users.py:update_user_info()`

**说明**：当前仅支持更新部分字段（username、email、is_active、role），不支持更新个人资料字段如 full_name、bio、avatar_url 等

---

#### DELETE `/api/v1/users/{user_id}`

**功能**：删除用户

**路径参数**：
- `user_id` (int) - 用户ID

**响应**（200 OK）：
```json
{
  "message": "User deleted successfully"
}
```

**认证要求**：需要 Bearer Token

**权限要求**：仅管理员（admin 角色）

**实现位置**：`app/api/routers/users.py:delete_user_info()`

---

### 3.3 文档管理接口

**路由文件**：`api/routers/documents.py`

#### GET `/api/v1/documents`

**功能**：获取当前用户的文档列表

**查询参数**：
- `skip` (int, 默认0) - 跳过记录数
- `limit` (int, 默认100) - 返回记录数

**响应**（200 OK）：
```json
[
  {
    "id": 1,
    "owner_id": 1,
    "title": "string",
    "content": "string",
    "status": "active",
    "created_at": "2024-01-01T00:00:00",
    "updated_at": "2024-01-01T00:00:00"
  }
]
```

**认证要求**：需要 Bearer Token

**权限要求**：只能查看自己的文档

**实现位置**：`app/api/routers/documents.py:get_documents_endpoint()`

---

#### POST `/api/v1/documents`

**功能**：创建文档

**请求体**（JSON）：
```json
{
  "title": "string",           // 必填
  "content": "string",         // 可选，默认空字符串
  "status": "active"           // 可选，默认active
}
```

**响应**（201 Created）：Document 对象

**认证要求**：需要 Bearer Token

**实现位置**：`app/api/routers/documents.py:create_document_endpoint()`

---

#### GET `/api/v1/documents/{document_id}`

**功能**：获取文档详情

**路径参数**：
- `document_id` (int) - 文档ID

**响应**（200 OK）：Document 对象

**认证要求**：需要 Bearer Token

**权限要求**：只能查看自己的文档

**实现位置**：`app/api/routers/documents.py:get_document_endpoint()`

---

#### PUT `/api/v1/documents/{document_id}`

**功能**：更新文档

**路径参数**：
- `document_id` (int) - 文档ID

**请求体**（JSON）：
```json
{
  "title": "string",           // 可选
  "content": "string",          // 可选
  "status": "string"           // 可选
}
```

**响应**（200 OK）：更新后的 Document 对象

**认证要求**：需要 Bearer Token

**权限要求**：只能更新自己的文档

**实现位置**：`app/api/routers/documents.py:update_document_endpoint()`

---

#### DELETE `/api/v1/documents/{document_id}`

**功能**：删除文档

**路径参数**：
- `document_id` (int) - 文档ID

**响应**（204 No Content）

**认证要求**：需要 Bearer Token

**权限要求**：只能删除自己的文档

**实现位置**：`app/api/routers/documents.py:delete_document_endpoint()`

---

#### GET `/api/v1/documents/{document_id}/versions`

**功能**：获取文档版本列表

**路径参数**：
- `document_id` (int) - 文档ID

**响应**（200 OK）：
```json
[
  {
    "id": 1,
    "version_number": 1,
    "user_id": 1,
    "summary": "string",
    "created_at": "2024-01-01T00:00:00"
  }
]
```

**认证要求**：需要 Bearer Token

**权限要求**：只能查看自己文档的版本

**实现位置**：`app/api/routers/documents.py:get_document_versions_endpoint()`

---

#### POST `/api/v1/documents/{document_id}/versions`

**功能**：创建文档版本

**路径参数**：
- `document_id` (int) - 文档ID

**查询参数**：
- `content` (string) - 内容快照，必填
- `summary` (string) - 变更摘要，可选，默认空字符串

**响应**（201 Created）：
```json
{
  "id": 1,
  "version_number": 1,
  "summary": "string",
  "created_at": "2024-01-01T00:00:00"
}
```

**认证要求**：需要 Bearer Token

**权限要求**：只能为自己的文档创建版本

**实现位置**：`app/api/routers/documents.py:create_document_version_endpoint()`

---

### 3.4 管理员接口

**路由文件**：`api/routers/admin.py`

**前缀**：所有接口均在 `/api/v1/admin` 下

#### 用户管理

- `GET /api/v1/admin/users` - 获取用户列表
- `GET /api/v1/admin/users/{user_id}` - 获取用户详情
- `PATCH /api/v1/admin/users/{user_id}/role` - 更新用户角色

#### 审计日志

- `GET /api/v1/admin/audit` - 获取审计日志列表
- `GET /api/v1/admin/audit/summary` - 审计日志聚合统计

#### 反馈管理

- `GET /api/v1/admin/feedback` - 获取用户反馈列表

#### 系统设置

- `GET /api/v1/admin/settings` - 获取系统设置列表
- `PUT /api/v1/admin/settings/{key}` - 更新系统设置

#### 系统监控

- `GET /api/v1/admin/monitoring/health` - 健康检查
- `GET /api/v1/admin/monitoring/system` - 系统信息（CPU、内存、磁盘）
- `GET /api/v1/admin/monitoring/application` - 应用统计
- `GET /api/v1/admin/monitoring/database` - 数据库统计
- `GET /api/v1/admin/monitoring/dashboard` - 监控仪表板汇总
- `GET /api/v1/admin/monitoring/metrics/{metric_name}` - 获取指标数据
- `GET /api/v1/admin/monitoring/metrics/{metric_name}/aggregation` - 获取指标聚合
- `POST /api/v1/admin/monitoring/cleanup` - 清理过期数据

#### 备份管理

- `GET /api/v1/admin/backup/list` - 列出备份
- `POST /api/v1/admin/backup/create` - 创建备份
- `GET /api/v1/admin/backup/{backup_name}` - 获取备份详情
- `POST /api/v1/admin/backup/restore` - 恢复备份
- `DELETE /api/v1/admin/backup/{backup_name}` - 删除备份
- `GET /api/v1/admin/backup/export/{table}` - 导出表
- `POST /api/v1/admin/backup/cleanup` - 清理旧备份

**认证要求**：所有接口均需要 Bearer Token + admin 角色

---

### 3.5 反馈接口

**路由文件**：`api/routers/feedback.py`

#### POST `/api/v1/feedback`

**功能**：提交用户反馈

**请求体**（JSON）：
```json
{
  "type": "bug|feature|other",
  "title": "反馈标题",
  "content": "反馈内容"
}
```

**认证要求**：需要 Bearer Token

---

### 3.6 通知接口

**路由文件**：`api/routers/notifications.py`

#### GET `/api/v1/notifications`

**功能**：获取当前用户的通知列表

**查询参数**：
- `unread_only` (bool, 默认false) - 是否只返回未读
- `skip` (int, 默认0) - 跳过记录数
- `limit` (int, 默认50) - 返回记录数

**认证要求**：需要 Bearer Token

---

#### PATCH `/api/v1/notifications/{notification_id}/read`

**功能**：标记通知为已读

---

#### POST `/api/v1/notifications/read_batch`

**功能**：批量标记通知已读

**请求体**（JSON）：
```json
{
  "notification_ids": [1, 2, 3]
}
```

---

### 3.7 实时聊天接口

**路由文件**：`api/routers/chat.py`

#### GET `/api/v1/documents/{document_id}/chat`

**功能**：获取文档聊天记录

**查询参数**：
- `limit` (int, 默认50) - 返回记录数
- `before_id` (int, 可选) - 获取此ID之前的消息

---

#### POST `/api/v1/documents/{document_id}/chat`

**功能**：发送聊天消息

**请求体**（JSON）：
```json
{
  "content": "消息内容"
}
```

---

#### DELETE `/api/v1/documents/{document_id}/chat/{message_id}`

**功能**：删除聊天消息（仅限消息发送者）

---

### 3.8 WebSocket 接口

**路由文件**：`api/routers/ws.py`

#### WebSocket `/api/v1/ws/documents/{document_id}`

**功能**：实时协作编辑连接

**连接地址**：`ws://<host>/api/v1/ws/documents/{document_id}`

**路径参数**：
- `document_id` (int) - 文档ID

**支持的消息类型**：

1. **内容更新消息**（客户端 → 服务器）：
```json
{
  "type": "content",
  "content": "文档内容"
}
```

2. **光标位置消息**（客户端 → 服务器）：
```json
{
  "type": "cursor",
  "cursor": {
    "position": 10
  }
}
```

**服务器发送的消息类型**：

1. **初始化消息**（连接建立时）：
```json
{
  "type": "init",
  "content": "文档初始内容"
}
```

2. **用户加入消息**（广播给其他用户）：
```json
{
  "type": "user_joined",
  "user_id": 1,
  "username": "string",
  "color": "#FF5733"
}
```

3. **内容更新广播**（广播给其他用户）：
```json
{
  "type": "content",
  "content": "更新后的文档内容",
  "user_id": 1
}
```

4. **光标位置广播**（广播给其他用户）：
```json
{
  "type": "cursor",
  "user_id": 1,
  "username": "string",
  "cursor": {
    "position": 10
  },
  "color": "#FF5733"
}
```

**认证要求**：支持通过查询参数传递 Token（可选），格式：`?token=<jwt_token>`

**实现位置**：`app/api/routers/ws.py:websocket_document_endpoint()`

**说明**：
- 连接建立后，服务器会发送文档初始内容
- 内容更新会实时保存到数据库并广播给房间内其他用户
- 光标位置会实时广播给其他用户，用于显示其他用户的光标
- 当前实现使用简单的全量内容同步，未使用 CRDT 算法

### 3.5 WebSocket 服务实现

**服务文件**：`app/services/websocket_service.py`

**实现类**：`ConnectionManager`

**核心功能**：
- 管理每个文档房间的 WebSocket 连接
- 广播消息给房间内其他用户
- 用户加入/离开通知
- 用户颜色分配（用于显示不同用户的光标和标识）

**数据结构**：
```python
active_connections: Dict[int, list]  # document_id -> list of connections with user info
```

**连接信息**：
- 每个连接包含 WebSocket 对象和完整的用户对象

**用户颜色分配**：
- 为每个用户分配固定颜色，确保用户在协作编辑时有独特标识
- 颜色列表：["#FF5733", "#33FF57", "#3357FF", "#F333FF", "#FF33A1", "#33FFF0", "#FFBD33", "#8D33FF"]

**当前实现问题**：
- WebSocket 连接中生成的用户 ID 是临时性的（基于时间戳），不是真实的认证用户
- ConnectionManager 类中的 connect 方法签名与实际调用不匹配
- WebSocket 端点未进行用户认证，所有人都可以连接任意文档

---

## 4. 内部模块与依赖关系

### 4.1 架构分层

项目采用**分层架构**，主要分为以下层次：

1. **Router 层**（`app/api/routers/`）
   - 职责：接收 HTTP 请求，参数验证，调用 Service 层，返回响应
   - 文件：`auth.py`, `users.py`, `documents.py`, `ws.py`

2. **Service 层**（`app/services/`）
   - 职责：封装业务逻辑，数据验证，事务控制（不直接 commit，由 Router 层控制）
   - 文件：`user_service.py`, `document_service.py`, `websocket_service.py`

3. **Model 层**（`models/`）
   - 职责：定义数据库模型和关系
   - 文件：`models/__init__.py`

4. **Schema 层**（`schemas/`）
   - 职责：定义请求/响应的数据结构，数据验证
   - 文件：`schemas/__init__.py`

5. **Core 层**（`app/core/`）
   - 职责：应用配置、安全认证、密码加密
   - 文件：`config.py`, `security.py`

6. **DB 层**（`app/db/`）
   - 职责：数据库连接、会话管理、初始化脚本
   - 文件：`session.py`, `init_db.py`, `init_permissions.py`

7. **CRDT 层**（`app/`）
   - 职责：冲突解决算法实现（当前未集成）
   - 文件：`crdt.py`

### 4.2 调用关系示例

**用户注册流程**：
```
POST /api/v1/auth/register
  → app/api/routers/auth.py:register()
    → app/services/user_service.py:create_user()
      → models/__init__.py:User (ORM 模型)
        → app/db/session.py:get_db() (数据库会话)
          → OpenGauss 数据库
```

**用户登录流程**：
```
POST /api/v1/auth/token
  → app/api/routers/auth.py:login()
    → app/core/security.py:verify_password()
    → app/core/security.py:create_access_token()
      → python-jose (JWT 生成)
```

**获取文档列表流程**：
```
GET /api/v1/documents
  → app/api/routers/documents.py:get_documents_endpoint()
    → app/core/security.py:get_current_user() (认证)
    → app/services/document_service.py:get_documents()
      → models/__init__.py:Document (ORM 查询)
        → app/db/session.py:get_db()
          → OpenGauss 数据库
```

**WebSocket 协作编辑流程**：
```
WebSocket /api/v1/ws/documents/{document_id}
  → app/api/routers/ws.py:websocket_document_endpoint()
    → app/services/websocket_service.py:ConnectionManager
      → 广播消息给房间内其他用户
      → 更新 models/__init__.py:Document.content
        → app/db/session.py (数据库更新)
```

### 4.3 数据流向

1. **请求处理**：HTTP Request → Router → Service → Model → Database
2. **响应返回**：Database → Model → Service → Router → HTTP Response
3. **WebSocket**：WebSocket Message → Router → Service → Broadcast → 其他客户端

### 4.4 注意事项

- **事务控制**：Service 层函数默认不提交事务（`commit=False`），由 Router 层统一控制提交和回滚
- **认证依赖**：大部分接口依赖 `app/core/security.py:get_current_user()` 进行 JWT 认证
- **日志记录**：关键操作（如用户查看、更新、删除）会记录到 `operation_logs` 表
- **WebSocket 认证**：当前 WebSocket 连接未强制认证，存在安全风险
- **文件路径**：注意项目中存在两套路径结构（`app/` 下和根目录下），实际使用的是 `app/` 目录下的结构

---

## 5. 外部服务与第三方 API

### 5.1 当前未集成的外部服务

根据代码分析，项目当前**未集成**以下外部服务：

**邮件服务**（用于验证码发送）：
- 配置项：未在 `app/core/config.py` 中定义相关配置项
- 用途：发送密码重置验证码
- 状态：TODO - 需要集成实际的邮件服务（如 SMTP、SendGrid、阿里云邮件推送等）

**短信服务**（用于验证码发送）：
- 配置项：未在 `app/core/config.py` 中定义相关配置项
- 用途：发送密码重置验证码
- 状态：TODO - 需要集成实际的短信服务（如阿里云短信、腾讯云短信等）

**文件存储服务**（用于头像上传）：
- 当前实现：本地文件系统存储（`uploads/` 目录）
- 配置项：`UPLOAD_DIR`（在 `app/core/config.py` 中配置为 "uploads"）
- 静态文件访问：`/static/uploads/` 路径映射到上传目录
- 状态：基础功能已实现，但头像上传接口未在当前路由中暴露
- 建议：生产环境迁移到对象存储服务（如阿里云 OSS、腾讯云 COS、AWS S3 等）

**权限管理服务**：
- 当前实现：基于数据库的 RBAC + ACL 模型
- 状态：模型已完整实现，但权限管理 API 路由未暴露
- 建议：添加权限管理接口，支持动态权限配置

### 5.2 第三方库依赖

**认证与安全**：
- `python-jose` - JWT Token 生成和验证
- `passlib` - 密码哈希（bcrypt）

**数据库**：
- `psycopg2-binary` - PostgreSQL/OpenGauss 数据库驱动

**Web 框架**：
- `fastapi` - Web 框架
- `uvicorn` - ASGI 服务器
- `websockets` - WebSocket 支持

**数据验证**：
- `pydantic` / `pydantic-settings` - 数据验证和配置管理
- `email-validator` - 邮箱格式验证

---

## 6. 配置与运行环境

### 6.1 配置文件

**主配置文件**：`app/core/config.py`

**配置项说明**：

```python
# 项目基本信息
PROJECT_NAME = "Collaborative Editor API"
VERSION = "1.0.0"
API_V1_STR = "/api/v1"  # API 版本前缀

# 数据库配置（通过环境变量 DATABASE_URL 覆盖）
DATABASE_URL = "postgresql+psycopg2://appuser:YH030500!@120.46.143.126:5432/editdb"

# JWT 配置
SECRET_KEY = "your-secret-key-change-in-production"  # 生产环境必须修改
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 30  # Token 有效期（分钟）

# 文件上传配置
UPLOAD_DIR = "uploads"  # 上传文件目录

# CORS 配置
BACKEND_CORS_ORIGINS = [
    "http://localhost",
    "http://127.0.0.1",
    "http://localhost:3000",
    "http://127.0.0.1:3000"
]
```

**注意**：
- 数据库连接字符串中包含实际密码，生产环境应使用环境变量
- 文件上传相关的 `MAX_UPLOAD_SIZE` 和 `ALLOWED_IMAGE_TYPES` 配置未在配置类中定义，但在代码中硬编码使用

### 6.2 环境变量

项目使用 `python-dotenv` 支持 `.env` 文件，可通过环境变量覆盖配置：

- `DATABASE_URL` - 数据库连接字符串
- `SECRET_KEY` - JWT 密钥（生产环境必须设置）
- `ACCESS_TOKEN_EXPIRE_MINUTES` - Token 有效期

### 6.3 数据库连接配置

**连接配置位置**：`app/db/session.py`

**连接池参数**：
- `pool_size`: 10（连接池大小）
- `max_overflow`: 20（最大溢出连接数）
- `pool_pre_ping`: True（连接前验证有效性）
- `echo`: True（开发环境打印 SQL，生产环境应设为 False）

**注意**：当前 `echo=True` 会打印所有 SQL 语句，生产环境应设为 False 以提高性能和安全性

### 6.4 运行环境

**开发环境**：
- 数据库：本地 OpenGauss 或 PostgreSQL
- 端口：默认 8000（可通过 uvicorn 参数修改）
- 调试：SQL 日志开启（`echo=True`）

**生产环境注意事项**：
- 必须修改 `SECRET_KEY`
- 设置 `echo=False` 关闭 SQL 日志
- 配置正确的 `BACKEND_CORS_ORIGINS`
- 使用环境变量管理敏感配置
- 配置日志系统（当前使用 Python logging）

---

## 7. 认证与权限

### 7.1 认证方式

**认证类型**：JWT Token（Bearer Token）

**认证实现位置**：`app/core/security.py`

**认证流程**：
1. 用户通过 `/api/v1/auth/token` 登录，提供用户名和密码
2. 服务器验证密码后生成 JWT Token
3. 客户端在后续请求的 `Authorization` 头中携带 Token：`Bearer <token>`
4. 服务器通过 `get_current_user()` 依赖函数验证 Token 并获取用户信息

**Token 结构**：
- 算法：HS256
- 载荷：`{"sub": "username", "exp": <过期时间>}`
- 有效期：默认 30 分钟（可通过配置修改）

**注意**：WebSocket 连接支持通过查询参数传递 Token，但当前未强制验证

### 7.2 权限模型

**权限类型**：基于角色的访问控制（RBAC）+ 访问控制列表（ACL）

**角色定义**：
- **admin**（管理员）：拥有所有权限
- **editor**（编辑者）：可以创建、读取、更新文档，查看用户信息
- **viewer**（查看者）：只能读取文档和查看用户信息

**权限检查优先级**（`services/permission_service.py:check_permission()`）：
1. 用户特定的 ACL（`acls` 表中 `user_id` 不为空）
2. 角色特定的 ACL（`acls` 表中 `role` 不为空）
3. 角色权限（`role_permissions` 表）
4. 默认角色权限（代码中硬编码）

**默认权限分配**（`db/init_permissions.py`）：

- **admin**：所有权限
  - read_document, write_document, update_document, delete_document
  - read_user, update_user, delete_user
  - manage_permissions, view_logs

- **editor**：
  - read_document, write_document, update_document
  - read_user

- **viewer**：
  - read_document
  - read_user

**权限检查位置**：
- `app/core/security.py:get_current_user()` - 基础认证
- `app/api/routers/users.py` - 用户操作权限检查（如删除用户需要 admin）
- `app/api/routers/documents.py` - 文档操作权限检查（只能操作自己的文档）

**注意**：权限服务（`permission_service.py`）在文档中提到但实际代码中未找到，权限检查主要在路由层硬编码实现

### 7.3 认证中间件

**OAuth2 方案**：`app/core/security.py:oauth2_scheme`

**Token URL**：`/api/v1/auth/token`

**依赖注入**：使用 FastAPI 的 `Depends()` 机制，在路由函数中通过 `current_user: UserSchema = Depends(get_current_user)` 获取当前用户

**注意**：WebSocket 端点未使用认证中间件，存在安全风险

---

## 8. 其他重要机制

### 8.1 操作日志系统

**实现位置**：`app/api/routers/users.py:log_operation()`

**记录内容**：
- 用户ID
- 操作类型（action）
- 资源类型和ID
- 操作描述
- IP 地址和 User-Agent

**记录的操作**：
- 查看用户信息
- 更新用户信息
- 删除用户
- 登录（在 `app/api/routers/auth.py` 中记录）

**存储位置**：`operation_logs` 表

**注意事项**：日志记录失败不会影响主业务流程，只记录错误日志

**问题**：当前登录操作未实际记录日志（代码中未找到相关实现）

### 8.2 文件上传机制

**实现位置**：未在当前路由文件中找到实现

**支持功能**：
- 头像上传（模型中已支持 `avatar_url` 字段）
- 文件类型验证（JPEG, PNG, GIF, WebP）
- 文件大小限制（默认 5MB）
- 文件存储路径：`uploads/`
- 静态文件访问：`/static/uploads/` 路径映射到上传目录

**配置项**（代码中使用但未在配置类中定义）：
- `UPLOAD_DIR`: 上传目录
- `MAX_UPLOAD_SIZE`: 最大文件大小
- `ALLOWED_IMAGE_TYPES`: 允许的图片类型

**状态**：基础配置已实现，但文件上传接口未暴露

### 8.3 数据库初始化

**初始化脚本**：`app/db/init_db.py`

**初始化内容**：
1. 创建所有数据表（通过 SQLAlchemy `create_all()`）
2. 初始化默认权限和角色权限（调用 `app/db/init_permissions.py:init_permissions()`）

**触发时机**：应用启动时（`app/main.py:on_startup()`）

**错误处理**：
- 数据库初始化失败会阻止应用启动
- 权限初始化：当前代码中未找到 `init_permissions.py` 文件，权限初始化可能未实现

### 8.4 WebSocket 连接管理

**实现位置**：`app/services/websocket_service.py:ConnectionManager`

**功能**：
- 管理每个文档房间的 WebSocket 连接
- 广播消息给房间内其他用户
- 用户加入/离开通知
- 用户颜色分配（用于显示不同用户的光标）

**数据结构**：
```python
active_connections: Dict[int, list]  # document_id -> list of connections with user info
```

**连接信息**：
- 每个连接包含 WebSocket 对象和完整的用户对象
- 用户颜色固定分配：基于用户ID取模，确保同一用户永远同一种颜色
- 颜色列表：["#FF5733", "#33FF57", "#3357FF", "#F333FF", "#FF33A1", "#33FFF0", "#FFBD33", "#8D33FF"]

**实现问题**：
- `connect` 方法签名与 WebSocket 端点调用不匹配
- WebSocket 端点生成的用户 ID 是临时的，不是真实认证用户
- 缺少用户认证机制

---

## 9. 项目入口与启动

### 9.1 应用入口

**主入口文件**：`app/main.py`

**应用对象**：`app`（FastAPI 实例）

**启动方式**：
```bash
# 使用 uvicorn
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 9.2 API 文档

**Swagger UI**：`http://localhost:8000/api/docs`

**ReDoc**：`http://localhost:8000/api/redoc`

### 9.3 静态文件服务

**静态文件目录**：`/static` → `static/` 目录

**上传文件目录**：`/static/uploads` → `uploads/` 目录

**模板文件目录**：`templates/` 目录

### 9.4 页面路由

**主页面**：`/` → `templates/index.html`（协作编辑器界面）

**测试页面**：`/test` → `templates/test_collab.html`（协作编辑器测试界面）

---

## 10. 注意事项与待完善功能

### 10.1 安全相关

1. **验证码发送**：验证码生成功能已实现，但邮件/短信服务未集成
2. **WebSocket 认证**：当前 WebSocket 连接未强制要求认证，存在安全风险
3. **SECRET_KEY**：生产环境必须修改默认密钥
4. **CORS 配置**：生产环境需要配置正确的允许来源
5. **数据库密码**：配置文件中包含明文密码，应使用环境变量

### 10.2 功能完善

1. **密码重置流程**：验证码和重置令牌机制已实现，但邮件/短信发送功能待集成
2. **头像上传接口**：用户模型支持头像URL，静态文件服务已配置，但上传接口未暴露
3. **权限管理 API**：权限和 ACL 模型已完整实现，但管理接口未暴露
4. **CRDT 算法集成**：已实现基础CRDT框架，但未在WebSocket协作中实际应用
5. **登录方式扩展**：当前仅支持用户名登录，不支持邮箱或手机号登录
6. **用户个人资料**：个人资料字段已定义，但更新接口未实现

### 10.3 性能优化

1. **N+1 查询**：文档版本查询已优化，使用 `get_document_version_count()` 避免 N+1
2. **连接池**：已配置连接池，参数可根据实际负载调整
3. **日志系统**：建议配置结构化日志（如 JSON 格式）便于后续分析
4. **SQL 日志**：生产环境应关闭 `echo=True` 以提高性能

### 10.4 代码问题

1. **WebSocket 实现**：ConnectionManager 类与实际调用不匹配
2. **认证一致性**：WebSocket 使用临时用户ID，与HTTP认证不一致
3. **初始化脚本**：`init_permissions.py` 文件缺失，权限初始化未实现
4. **事务控制**：部分操作缺少错误处理和回滚机制

---

## 11. 前端协作机制详解

### 11.1 实时协作流程

**前端文件**：`static/editor.js`

**连接建立**：
```javascript
// 支持Token认证的WebSocket连接
const tokenPart = token ? `?token=${token}` : '';
ws = new WebSocket(`${protocol}//${location.host}/api/v1/ws/documents/${doc_id}${tokenPart}`);
```

**内容同步机制**：
1. **增量同步**：使用本地内容缓存(`localContent`)对比，只在内容实际变化时发送
2. **防抖优化**：100ms防抖延迟，避免频繁网络请求
3. **消息类型**：
   - `content` - 文档内容更新
   - `cursor` - 光标位置同步
   - `init` - 初始内容加载
   - `user_joined` - 用户加入通知

**光标同步实现**：
```javascript
// 精确光标位置计算（支持中文、换行）
function drawCursor(user_id, username, position, color) {
    // 使用Canvas API精确测量字符宽度
    const charWidth = measureTextWidth("测", editor);
    // 支持多行文本和中文混合显示
    const lines = textBefore.split("\n");
    const lineNum = lines.length - 1;
    const colNum = lines[lines.length - 1].length;
}
```

**性能优化**：
- **防抖机制**：输入事件100ms延迟发送
- **增量更新**：只在内容变化时发送消息
- **Canvas测量**：精确计算字符宽度，支持中英文混排
- **事件优化**：`selectionchange` 事件50ms延迟处理

**问题**：
- WebSocket 连接未验证用户身份
- 用户ID是临时生成的，与认证系统不一致
- 缺少断线重连机制

### 11.2 CRDT 算法实现

**实现文件**：`app/crdt.py`

**核心数据结构**：
```python
class CRDT:
    def __init__(self):
        self.sequence = []  # 每个元素: {"id": (client, ts), "char": "A", "deleted": False}
```

**操作类型**：
1. **插入操作**：
   ```python
   def insert(self, index, char, client_id):
       op_id = (client_id, time.time())  # 唯一操作ID
       element = {"id": op_id, "char": char, "deleted": False}
       return {"type": "insert", "id": op_id, "index": index, "char": char}
   ```

2. **删除操作**：
   ```python
   def delete(self, index):
       self.sequence[index]["deleted"] = True  # 标记删除，不物理删除
       return {"type": "delete", "id": self.sequence[index]["id"]}
   ```

3. **操作合并**：
   ```python
   def merge(self, ops):
       for op in ops:
           self.apply(op)  # 按序应用操作
   ```

**当前状态**：
- ✅ 基础CRDT框架已实现
- ⚠️ 未在WebSocket协作中实际应用
- ⚠️ 当前使用简单的全量内容同步
- ⚠️ CRDT 算法实现过于简化，缺少真正的冲突解决机制
- 🔧 可优化为真正的CRDT冲突解决机制

**建议改进**：
1. 集成CRDT到WebSocket消息处理
2. 实现操作转换(OT)或真正的CRDT算法
3. 优化网络传输，只同步操作而非全量内容
4. 添加向量时钟或版本向量确保操作顺序一致性

---

### 12. 依赖包清单

**核心依赖**（`requirements.txt`）：
- `fastapi==0.121.2` - Web 框架
- `uvicorn==0.38.0` - ASGI 服务器
- `SQLAlchemy==2.0.44` - ORM
- `psycopg2-binary==2.9.11` - PostgreSQL 驱动
- `python-jose[cryptography]==3.5.0` - JWT 处理
- `passlib[bcrypt]==1.7.4` - 密码加密
- `pydantic-settings==2.0.3` - 配置管理
- `websockets==15.0.1` - WebSocket 支持
- `python-multipart==0.0.12` - 文件上传支持
- `email-validator==2.3.0` - 邮箱验证
- `python-dotenv==1.2.1` - 环境变量管理
- `anyio==4.11.0` - 异步 I/O
- `jinja2==3.1.4` - 模板引擎

**注意**：
- `python-jose` 需要加密支持，使用了 `[cryptography]` 额外包
- `passlib` 需要 bcrypt 支持，使用了 `[bcrypt]` 额外包

---

## 13. 文件路径索引

**关键文件路径**：

- 应用入口：`app/main.py`
- 配置：`app/core/config.py`
- 安全认证：`app/core/security.py`
- 数据库连接：`app/db/session.py`
- 数据库初始化：`app/db/init_db.py`
- 权限初始化：`app/db/init_permissions.py`（文件缺失）
- 数据模型：`models/__init__.py`
- 数据验证：`schemas/__init__.py`
- 认证路由：`app/api/routers/auth.py`
- 用户路由：`app/api/routers/users.py`
- 文档路由：`app/api/routers/documents.py`
- WebSocket 路由：`app/api/routers/ws.py`
- 用户服务：`app/services/user_service.py`
- 文档服务：`app/services/document_service.py`
- WebSocket 服务：`app/services/websocket_service.py`
- CRDT 算法：`app/crdt.py`
- 前端编辑器：`static/editor.js`
- 前端测试页面：`templates/test_collab.html`
- 主页面：`templates/index.html`
- 依赖清单：`requirements.txt`

---

**文档生成时间**：2024年
**项目版本**：1.1.0
**最后更新**：2025年1月 - 新增认证、管理和监控功能
**更新内容**：
- 新增密码重置接口（/password-reset/request, /password-reset/verify）
- 新增验证码登录接口（/login/code/request, /login/code/verify）
- 新增 OAuth2 第三方登录接口（GitHub/Google/微信）
- 新增双因素认证（2FA）相关接口
- 新增管理员接口（用户管理、审计日志、系统设置）
- 新增系统监控接口（健康检查、系统信息、指标收集）
- 新增备份管理接口（创建、恢复、导出）
- 新增实时聊天接口（文档内聊天功能）
- 新增通知系统接口（通知列表、标记已读）
- 新增反馈提交接口
- 完善文档 CRUD 路由（GET/PUT/DELETE）
- 新增模板管理路由（CRUD 操作）
- 新增协作者管理路由（添加/移除）
- 代码重构：提取公共工具函数到 app/core/utils.py
- 清理未使用的导入和废弃代码